import React from 'react';
import { Box, Typography } from '@mui/material';

function Footer() {
  return (
    <Box display="flex" justifyContent={'center'}>
      <Typography variant="button">
        ~~~~~~~[upcoming Footer Section]~~~~~~~
      </Typography>
    </Box>
  );
}

export default Footer;
